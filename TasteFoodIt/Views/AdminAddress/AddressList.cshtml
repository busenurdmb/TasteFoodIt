@model List<TasteFoodIt.Entity.Address>
@{
    ViewBag.Title = "AddressList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}
<div class="container-fluid" id="container-wrapper">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800">@ViewBag.name Listesi</h1>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="./">Anasayfa</a></li>
            <li class="breadcrumb-item">@ViewBag.name</li>
            <li class="breadcrumb-item active" aria-current="page">@ViewBag.name İşlemleri</li>
        </ol>
    </div>
    <div class="row">
        <div class="col-lg-12 mb-4">
            <!-- Simple Tables -->
            <div class="card">
                <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between">
                    <h6 class="m-0 font-weight-bold text-primary">@ViewBag.name</h6>
                </div>
                @if (Model.Count == 0)
                {
                    <div class="card-body">
                        <div class="alert alert-info" role="alert">
                            <h4 class="alert-heading">Hakkımda kısmında bir veri bulunmamaktadır!</h4>
                        </div>
                        <a href="#" class="btn btn-primary">Hakkımda Kısmına veri ekle</a>
                    </div>
                }
                else
                {
                    <div class="alert alert-light alert-dismissible" role="alert">
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">Uyarıyı kapat</span>
                        </button>
                        <h6><i class="fas fa-exclamation-triangle"></i><b> Bilgi!</b></h6>
                        Tema bütünlüğünün bozulmaması için bir veriden başka veri girmeyiniz
                    </div>

                    <div class="table-responsive">
                        <table class="table align-items-center table-flush">
                            <thead class="thead-light">
                                <tr>
                                    <th>*</th>
                                    <th>Email</th>
                                    <th> Açıklaması</th>
                                    <th>Telefon</th>
                                    <th>Sil</th>
                                    <th>Güncelle</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td><a href="#">@item.AddressId</a></td>
                                        <td>@item.Email</td>
                                        <td>@item.Description</td>
                                        <td>
                                           @item.Phone
                                        </td>
                                        <td><a href="/AdminAddress/DeleteAdress/@item.AddressId" class="badge badge-danger">Sil</a></td>
                                        <td><a href="/AdminAddress/UpdateAddress/@item.AddressId" class="btn btn-sm btn-success">Güncelle</a></td>
                                    </tr>
                                }


                            </tbody>
                        </table>

                    </div>
                }

                <div class="card-footer"></div>
            </div>
        </div>
    </div>

    <!--Row-->


</div>